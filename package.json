{
  "name": "zprint-admin",
  "homepage": "https://wanyutang.github.io/zprint-admin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "export": "nuxt export",
    "serve": "nuxt serve",
    "test": "jest",
    "generate": "rm -r dist && DEPLOY_ENV=GH_PAGES nuxt build && DEPLOY_ENV=GH_PAGES nuxt export",
    "deploy": "npm run generate && gh-pages -d dist"
  },
  "dependencies": {
    "nuxt": "^2.13.0"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.11.2",
    "@vue/test-utils": "^1.0.3",
    "babel-core": "7.0.0-bridge.0",
    "babel-jest": "^26.0.1",
    "gh-pages": "^3.1.0",
    "jest": "^26.0.1",
    "vue-jest": "^3.0.4"
  }
}
